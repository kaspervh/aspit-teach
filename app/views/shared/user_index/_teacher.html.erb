<tr>
  <th>Brugernavn</th>
  <th>Email</th>
  <th>Optioner</th>
  <th colspan="3"></th>
</tr>
</thead>

<tbody>
  <% @grade_students.each do |user| %>
  <tr>
    <td><strong><%= user.username %></strong></td>
    <td><strong><%= user.email %></strong></td>
  
    <td><%=link_to "Dagbogsnotater", diary_entries_path(user_id: user.id), class: "btn btn-secondary" %></td>        
  </tr>
    <% end %>
  </tbody>
</table>

<br>