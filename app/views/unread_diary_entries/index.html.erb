<p id="notice"><%= notice %></p>

<h3>Alle dagbogsnotater</h3>

<table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>Oprettelses dato</th>
      <th>Forfatter</th>
      <th>Optioner</th>
      <th>Læst?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @diary_entries.reverse.each do |diary_entry| %>
      <tr>
        <td><strong><%= diary_entry.created_at %></strong></td>
        <td><strong><%= diary_entry.author_name %></strong></td>

        <td><%= link_to 'Vis', diary_entry, class: "btn btn-secondary" %></td>
        <td>
        <%if diary_entry.read?(@current_user)%>   
          <i class="fa fa-check-square fa-4" aria-hidden="true"/>
        <%else%>
          <i class="fa fa-minus-square fa-4" aria-hidden="true"/>
        <%end%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
