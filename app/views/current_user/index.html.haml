%h1 Elever
%table.table
  %thead.thead-inverse
    %tr
      %th Brugernavn
      %th Email
      %th Optioner
      %th{:colspan => "3"}
  %tbody
    - @users.each do |user|
      %tr
        %td= user.username
        %td= user.email
        %td= link_to "vælg", user, class: "btn btn-secondary"
%br/
