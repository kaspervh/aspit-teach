<%= form_for [@grade, @scheduel] do |f| %>
  <% if @scheduel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scheduel.errors.count, "error") %> prohibited this scheduel from being saved:</h2>

      <ul>
      <% scheduel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :grade_id, value: @grade_id %>
  </div>
  <br>
  <div class="field">
    <strong><%= f.label :Upload_ugeskema %></strong>
    <br>
    <%= f.file_field :image%>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Gem", class: "btn btn-secondary" %>
  </div>
  <br>
<% end %>
