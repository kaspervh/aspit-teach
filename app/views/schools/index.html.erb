<p id="notice"><%= notice %></p>

<h3>Skoler</h3>

<table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>Name</th>
      <th>Optioner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @schools.each do |school| %>
      <tr>
        <td><strong><%= school.name %></strong></td>
                
        <td><%= link_to 'Rediger', edit_school_path(school), class: "btn btn-secondary" %>
            <%= link_to 'Vis', school, class: "btn btn-secondary" %>
            <%= link_to 'Slet', school, method: :delete, data: { confirm: 'Er du sikker på at du vil slette denne skole?' }, class: "btn btn-danger"%></td>
        </tr>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ny skole', new_school_path, class: "btn btn-secondary" %>
<%= link_to 'Ny skole klasse', new_grade_path, class: "btn btn-secondary" %>