<p id="notice"><%= notice %></p>

<h1>Skoler</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @schools.each do |school| %>
      <tr>
        <td><h4><%= school.name %><h4></td>
        <tr>
        
        <td><%= link_to 'Rediger', edit_school_path(school), class: "btn btn-success" %>
        <%= link_to 'Vis', school, class: "btn btn-success" %></td>
        <td><%= link_to 'Slet', school, method: :delete, data: { confirm: 'Er du sikker på at du vil slette denne skole?' }, class: "btn btn-success"%></td>
        <td></td>
        </tr>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ny skole', new_school_path, class: "btn btn-success" %>
<%= link_to 'Ny skole klasse', new_grade_path, class: "btn btn-success" %>