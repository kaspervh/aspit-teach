<%= form_for(instruction) do |f| %>
  <% if instruction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instruction.errors.count, "error") %> prohibited this instruction from being saved:</h2>

      <ul>
      <% instruction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong><%= f.label :Emne %></strong>
    <br>
    <%= f.collection_select :subject_id, @subjects, :id, :name%>
  </div>
  <br>
  <div class="field">
    <strong><%= f.label :Navn %></strong>
    <br>
    <%= f.text_field :name %>
  </div>
  <br>
  <div class="field">
    <strong><%= f.label :Indhold %></strong>
    <br>
    <%= f.cktext_area :content %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Gem", class: "btn btn-secondary" %>
  </div>
<% end %>
