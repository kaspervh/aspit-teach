%p#notice= notice
%h3 Beskeder
%table.table
  %thead.thead-inverse
    %tr
      %th Oprettelses dato
      %th Forfatter
      %th Optioner
      %th Læst?
      %th{:colspan => "3"}
  %tbody
    - @messages.each do |message|
      %tr
        %strong
          %td= message.created_at
        %strong
          %td= message.author_name
        %td= link_to 'Vis', message_path(message), class: "btn btn-secondary"
        %td
          - if message.read?(@current_user)
            %i.fa.fa-check-square.fa-4{"aria-hidden" => "true"}
          - else
            %i.fa.fa-minus-square.fa-4{"aria-hidden" => "true"}
%br/
